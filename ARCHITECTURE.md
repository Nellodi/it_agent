# 🏗️ Архитектура IT Ecosystem Bot

## 📐 Структура проекта

```
it_ecosystem_bot/
├── app.py                          # Главный файл для запуска бота
├── config.py                       # Конфигурация
├── database.py                     # Функции БД (20+ функций)
├── requirements.txt                # Зависимости
│
├── handlers/                       # Обработчики событий
│   ├── __init__.py
│   ├── auth.py                    # Авторизация пользователя
│   ├── start.py                   # Команда /start
│   ├── profile.py                 # Профиль пользователя (НОВОЕ)
│   ├── tickets.py                 # Создание заявок
│   ├── admin.py                   # Регистрация администраторов
│   ├── admin_tickets.py           # Управление заявками (НОВОЕ)
│   ├── equipment.py               # Управление оборудованием (НОВОЕ)
│   ├── workplaces.py              # Управление РМ (НОВОЕ)
│   └── ...
│
├── keyboards/                      # Клавиатуры (инлайн и обычные)
│   ├── __init__.py
│   ├── common.py                  # Основные меню (ОБНОВЛЕНО)
│   ├── main_menu.py               # Главное меню
│   ├── auth_kb.py                 # Клавиатуры авторизации
│   └── ...
│
├── utils/                         # Утилиты
│   ├── __init__.py
│   ├── auth_checks.py            # Проверка ролей
│   ├── helpers.py                # Вспомогательные функции
│   ├── excel_parser.py           # Парсинг Excel
│   └── inventory_generator.py    # Генерация инв.номеров (НОВОЕ)
│
├── states/                       # FSM состояния
│   ├── __init__.py
│   └── ...
│
├── office_db/                    # База данных (папка)
│   └── (пусто или файлы данных)
│
├── it_ecosystem.db              # SQLite база данных
│
├── IMPLEMENTATION_PHASE_1.md    # Документация функционала
├── CHANGELOG.md                 # История изменений
├── QUICK_START.md              # Краткая справка
├── DEPLOYMENT_GUIDE.md         # Руководство развертывания
└── ARCHITECTURE.md             # Этот файл
```

---

## 🔄 Архитектура данных

### Уровни приложения

```
┌─────────────────────────────────────────────────────────┐
│                  TELEGRAM USERS (UI Layer)               │
└────────────────┬────────────────────────────────────────┘
                 │
┌─────────────────────────────────────────────────────────┐
│            AIOGRAM BOT (Handler Layer) 🤖               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ auth.py      │  │ profile.py   │  │ tickets.py   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ admin.py     │  │admin_tickets │  │ equipment.py │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │ workplaces.py                                     │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────┬────────────────────────────────────────┘
                 │
┌─────────────────────────────────────────────────────────┐
│          UTILITIES (Business Logic Layer)               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ auth_checks  │  │ helpers      │  │inventory_gen │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────┬────────────────────────────────────────┘
                 │
┌─────────────────────────────────────────────────────────┐
│        DATABASE LAYER (database.py)                     │
│  20+ функций для работы с SQLite БД                    │
└────────────────┬────────────────────────────────────────┘
                 │
┌─────────────────────────────────────────────────────────┐
│        SQLITE DATABASE (Data Layer)                     │
│  8 таблиц: users, tickets, equipment, workplaces...    │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Таблицы БД и связи

```
authorized_users (PK: telegram_id)
    │
    ├── 1 to Many ──→ tickets (FK: user_id)
    │                     │
    │                     ├── 1 to Many ──→ ticket_history (FK: ticket_id)
    │                     │
    │                     └── FK: admin_id ──┐
    │                                         │
    ├── 1 to 1 ──→ sys_admins                 │
    │                                         ├─ (Self reference)
    ├── 1 to Many ──→ equipment (FK: user_id) │
    │                     │
    │                     ├── 1 to Many ──→ equipment_history
    │                     │
    │                     └── FK: workplace_id
    │
    ├── 1 to 1 ──→ licenses (FK: assigned_to)
    │
    └── 1 to Many ──→ workplaces
```

---

## 🔐 Система ролей и прав доступа

```
┌─────────────────────────────────────────────────────────┐
│ UNAUTHORIZED USER (Роли: нет)                          │
│ • Только команды /start и /login                       │
└─────────────────────────────────────────────────────────┘
                         │
                    Авторизация
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│ REGULAR USER (role = 'user')                           │
│ • Создание заявок                                      │
│ • Просмотр своих заявок                                │
│ • Просмотр своего профиля                              │
│ • Просмотр своего оборудования                          │
│ • Оценка заявок                                        │
└─────────────────────────────────────────────────────────┘
                         │
              Назначение администратора
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│ ADMIN (role = 'admin')                                 │
│ • Все права пользователя                               │
│ • Просмотр всех заявок                                 │
│ • Управление статусами заявок                          │
│ • Управление оборудованием (CRUD)                      │
│ • Управление рабочими местами                          │
│ • Просмотр рейтинга                                    │
└─────────────────────────────────────────────────────────┘
                         │
           Специальное назначение (в коде)
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│ SUPER_ADMIN (проверка в auth_checks.py)               │
│ • Все права администратора                             │
│ • Регистрация новых администраторов                    │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Потоки данных

### Создание заявки
```
User Input (text, category, priority)
         │
         ▼
tickets.py handler
         │
         ▼
validate data
         │
         ▼
database.save_new_ticket()
         │
         ▼
SQLite: INSERT INTO tickets
         │
         ▼
Notify admins
```

### Управление заявкой
```
Admin Input (select ticket, change status, add comment)
         │
         ▼
admin_tickets.py handler
         │
         ▼
database.update_ticket_status()
         │
         ▼
SQLite: UPDATE tickets + INSERT INTO ticket_history
         │
         ▼
Show confirmation to admin
```

### Управление оборудованием
```
Admin Input (category, model, serial)
         │
         ▼
equipment.py handler
         │
         ▼
inventory_generator.generate_inventory_number()
         │
         ▼
database.create_equipment()
         │
         ▼
SQLite: INSERT INTO equipment
         │
         ▼
Equipment ready for assignment
```

---

## 🎯 Обработка событий (Event Flow)

```
Message/Callback from Telegram
         │
         ▼
Dispatcher (aiogram)
         │
         ├─ Check Update Type (message/callback)
         │
         ├─ Find matching Router & Handler
         │
         ├─ Apply Filters (Command, StateFilter, etc.)
         │
         ▼
Execute Handler Function
         │
         ├─ Check User Role (auth_checks)
         │
         ├─ Validate Input Data
         │
         ├─ Call Database Functions
         │
         ├─ Update FSM State (if needed)
         │
         ├─ Generate Response
         │
         └─ Send to User
```

---

## 💾 Жизненный цикл данных

### Заявка
```
Created (status='open')
    │
    ├─ Assigned to Admin (admin_id set)
    │
    ├─ Changes Status multiple times
    │  (each change → ticket_history entry)
    │
    └─ Closed (status='closed' or 'await_rating')
       │
       └─ Rating (if applicable)
          │
          └─ Final Closure
```

### Оборудование
```
Created (status='available')
    │
    ├─ Assigned to User (status='assigned')
    │  (equipment_history entry)
    │
    ├─ May be reassigned multiple times
    │  (each reassignment → equipment_history)
    │
    └─ Optionally Deleted
```

---

## 🔌 API взаимодействие

### Telegram Bot API (через aiogram)
```
app.py ──┬─ dp.include_router(auth.router)
         ├─ dp.include_router(profile.router)
         ├─ dp.include_router(tickets.router)
         ├─ dp.include_router(admin.router)
         ├─ dp.include_router(admin_tickets.router)
         ├─ dp.include_router(equipment.router)
         ├─ dp.include_router(workplaces.router)
         │
         └─ dp.start_polling(bot)
```

---

## 📈 Масштабируемость

### Текущее решение:
- SQLite для локального хранилища
- Асинхронные операции (asyncio.to_thread)
- FSM для управления состояниями

### Для масштабирования:
- Перейти на PostgreSQL
- Кэширование (Redis)
- Микросервисная архитектура
- Очереди (Celery)

---

## 🛡️ Безопасность

### Текущие меры:
1. **Проверка ролей** - все операции проверяют роль пользователя
2. **Валидация данных** - все входные данные валидируются
3. **Логирование** - все действия логируются в БД
4. **Подтверждение** - критические операции требуют подтверждения

### Рекомендации:
1. Использовать environment variables для чувствительных данных
2. Регулярно архивировать БД
3. Настроить логирование ошибок
4. Использовать HTTPS для webhook (если будет развёрнут)

---

## 📚 Зависимости и их роли

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| aiogram | 3.10.0 | Telegram Bot API Client |
| python-dotenv | 1.0.0 | Загрузка переменных окружения |
| openpyxl | 3.1.2 | Парсинг Excel файлов |
| pandas | 2.1.4 | Обработка данных |

---

## 🔧 Расширение функционала

### Добавление нового обработчика
1. Создать новый файл в `handlers/` (например, `reports.py`)
2. Импортировать и создать router
3. Добавить функции-обработчики
4. Экспортировать в `handlers/__init__.py`
5. Зарегистрировать в `app.py`

### Добавление новых функций БД
1. Добавить функцию в `database.py`
2. Использовать формат: `async def function_name():`
3. Обернуть в `asyncio.to_thread()`
4. Добавить логирование

### Добавление новых таблиц
1. Создать CREATE TABLE в `init_db()`
2. Добавить функции для работы с таблицей
3. Добавить связи (FK) если нужны

---

## 📊 Производительность и оптимизация

### Текущие оптимизации:
- Асинхронная обработка (async/await)
- Кэширование ролей в памяти (можно добавить)
- Индексы на часто используемые колонки (рекомендуется добавить)

### Возможные улучшения:
```sql
-- Индексы для оптимизации запросов
CREATE INDEX idx_tickets_status ON tickets(status);
CREATE INDEX idx_tickets_user_id ON tickets(user_id);
CREATE INDEX idx_equipment_user_id ON equipment(user_id);
CREATE INDEX idx_equipment_status ON equipment(status);
```

